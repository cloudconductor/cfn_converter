require 'spec_helper'

describe CfnConverter do
  it 'has a version number' do
    expect(CfnConverter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
